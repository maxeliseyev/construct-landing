# technical

Edit the Russian only — the `##` heading is the key the site looks the
string up by, and the `>` line is the English original for reference.
When you are done: `python3 scripts/ru-review.py import`

## technical.intro  ·  HTML allowed

> The short version is on the front page. This is the long one: the transport, the crypto stack, and what the server can and cannot see.

Короткая версия — на главной. Здесь длинная: транспорт, криптографический стек и то, что сервер может и чего не может увидеть.

## technical.title  ·  plain text

> How it works

Как устроено
